import { ComponentProps, FunctionComponent, ReactNode } from 'react';
import { Icon } from './Icon';
interface SelectProps {
    disabled: boolean;
    inProgress: boolean;
}
declare const Selector: import("styled-components").StyledComponent<"select", any, SelectProps, never>;
interface OptionProps {
    label: string;
    value: string;
}
interface Props {
    options: OptionProps[];
    value: string;
    appearance?: 'default' | 'tertiary';
    label: string;
    hideLabel?: boolean;
    error?: ReactNode;
    icon?: ComponentProps<typeof Icon>['icon'];
    className?: string;
    inProgress?: boolean;
    disabled?: boolean;
}
export declare const Select: FunctionComponent<Props & ComponentProps<typeof Selector>>;
export {};
//# sourceMappingURL=Select.d.ts.map